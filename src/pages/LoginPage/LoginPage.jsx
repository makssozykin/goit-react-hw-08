import { LoginForm } from '../../components/LoginForm/LoginForm';

const LoginPage = () => {
  return (
    <div>
      <title>Login</title>
      <LoginForm />
    </div>
  );
};

export default LoginPage;
